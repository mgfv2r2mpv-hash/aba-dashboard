# Claude API key for scheduling solutions
CLAUDE_API_KEY=your_api_key_here

# Port for backend server (default: 5000)
PORT=5000

# Default encryption password for Excel files
# If not set, a random password is generated and shown in the UI
EXCEL_PASSWORD=change_me_to_a_secure_password
