import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.abascheduling',
  appName: 'ABA Scheduling Assistant',
  webDir: 'dist-client'
};

export default config;
